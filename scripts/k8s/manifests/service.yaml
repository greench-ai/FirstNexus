apiVersion: v1
kind: Service
metadata:
  name: nexusclaw
  labels:
    app: nexusclaw
spec:
  type: ClusterIP
  selector:
    app: nexusclaw
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
