apiVersion: v1
kind: ConfigMap
metadata:
  name: FirstNexus-config
  labels:
    app: FirstNexus
data:
  FirstNexus.json: |
    {
      "gateway": {
        "mode": "local",
        "bind": "loopback",
        "port": 18789,
        "auth": {
          "mode": "token"
        },
        "controlUi": {
          "enabled": true
        }
      },
      "agents": {
        "defaults": {
          "workspace": "~/.FirstNexus/workspace"
        },
        "list": [
          {
            "id": "default",
            "name": "FirstNexus Assistant",
            "workspace": "~/.FirstNexus/workspace"
          }
        ]
      },
      "cron": { "enabled": false }
    }
  AGENTS.md: |
    # FirstNexus Assistant

    You are a helpful AI assistant running in Kubernetes.
