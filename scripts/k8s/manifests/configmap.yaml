apiVersion: v1
kind: ConfigMap
metadata:
  name: nexusclaw-config
  labels:
    app: nexusclaw
data:
  nexusclaw.json: |
    {
      "gateway": {
        "mode": "local",
        "bind": "loopback",
        "port": 18789,
        "auth": {
          "mode": "token"
        },
        "controlUi": {
          "enabled": true
        }
      },
      "agents": {
        "defaults": {
          "workspace": "~/.nexusclaw/workspace"
        },
        "list": [
          {
            "id": "default",
            "name": "NexusClaw Assistant",
            "workspace": "~/.nexusclaw/workspace"
          }
        ]
      },
      "cron": { "enabled": false }
    }
  AGENTS.md: |
    # NexusClaw Assistant

    You are a helpful AI assistant running in Kubernetes.
